# Module mobilefoundation-notifications

Provider-neutral notification contracts and helpers.

# Package com.norbertotaveras.mobilefoundation.notifications

Defines notification channel models, topic models, token models, permission state, payload parsing, and deep link extraction from notification payloads.

Provider-specific push behavior belongs in modules such as `notifications-firebase`.
