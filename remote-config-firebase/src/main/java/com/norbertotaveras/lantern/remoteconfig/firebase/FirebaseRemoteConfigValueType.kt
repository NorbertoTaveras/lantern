package com.norbertotaveras.lantern.remoteconfig.firebase

/**
 * Expected Firebase Remote Config value type for a key.
 */
enum class FirebaseRemoteConfigValueType {
    Boolean,
    Double,
    Long,
    String
}
